# Agent / tooling internals (never commit)
.claude/
.codegraph
.omo/

# Personal / sensitive artifacts (never commit)
# Exception: tracking/*.csv are import-ready TEMPLATES (example rows only), safe to commit.
*.csv
!tracking/*.csv
exports/
screenshots/
*.xlsx
